NEWSLETTER_CALENDAR_URL=https://calendly.com/your-name/diagnosis-call

# ==========================================
# SUPABASE (ForgeBuilds CRM project — separate from Search4Jobs)
# ==========================================
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never expose to the browser / NEXT_PUBLIC_ vars.
SUPABASE_SERVICE_ROLE_KEY=

# ==========================================
# TELEGRAM BOT WORKSHOP (/workshops/telegrambot)
# ==========================================
# WhatsApp community invite link shown after a successful registration.
WHATSAPP_WORKSHOP_INVITE_URL=

# Password-gated admin view at /workshops/telegrambot/admin.
ADMIN_DASHBOARD_PASSWORD=
# Random secret used to sign the admin session cookie (e.g. `openssl rand -hex 32`).
ADMIN_SESSION_SECRET=
